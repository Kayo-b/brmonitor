// Code generated by protoc-gen-ts-server. DO NOT EDIT.
// source: osnit/v1/service.proto

export interface ListFeedRequest {
  timeframe: string;
  regions: string[];
  themes: string[];
  sourceTypes: string[];
  from: number;
  to: number;
  verificationStatus: string;
  limit: number;
  cursor: string;
  tags: string[];
  refresh: boolean;
}

export interface ListFeedResponse {
  items: OsnitItem[];
  nextCursor: string;
  total: number;
  refreshedAt: number;
}

export interface OsnitItem {
  id: string;
  sourceType: OsnitSourceType;
  sourceName: string;
  sourceHandle: string;
  url: string;
  title: string;
  text: string;
  publishedAt: number;
  ingestedAt: number;
  themes: string[];
  regions: string[];
  tags: string[];
  verificationStatus: OsnitVerificationStatus;
  corroborationCount: number;
  confidence: number;
  claimHash: string;
  highTrustReference: boolean;
}

export interface SearchItemsRequest {
  q: string;
  timeframe: string;
  regions: string[];
  themes: string[];
  sourceTypes: string[];
  from: number;
  to: number;
  verificationStatus: string;
  limit: number;
  cursor: string;
  tags: string[];
  refresh: boolean;
}

export interface SearchItemsResponse {
  items: OsnitItem[];
  nextCursor: string;
  total: number;
  refreshedAt: number;
}

export interface GetItemRequest {
  id: string;
}

export interface GetItemResponse {
  item?: OsnitItem;
}

export interface ListSourcesRequest {
  sourceType: string;
  region: string;
}

export interface ListSourcesResponse {
  sources: OsnitSource[];
  refreshedAt: number;
}

export interface OsnitSource {
  id: string;
  name: string;
  sourceType: OsnitSourceType;
  url: string;
  query: string;
  region: string;
  theme: string;
  enabled: boolean;
  highTrust: boolean;
}

export type OsnitSourceType = "OSNIT_SOURCE_TYPE_UNSPECIFIED" | "OSNIT_SOURCE_TYPE_RSS" | "OSNIT_SOURCE_TYPE_X" | "OSNIT_SOURCE_TYPE_TELEGRAM";

export type OsnitVerificationStatus = "OSNIT_VERIFICATION_STATUS_UNSPECIFIED" | "OSNIT_VERIFICATION_STATUS_UNVERIFIED" | "OSNIT_VERIFICATION_STATUS_PARTIALLY_VERIFIED" | "OSNIT_VERIFICATION_STATUS_VERIFIED";

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ServerContext {
  request: Request;
  pathParams: Record<string, string>;
  headers: Record<string, string>;
}

export interface ServerOptions {
  onError?: (error: unknown, req: Request) => Response | Promise<Response>;
  validateRequest?: (methodName: string, body: unknown) => FieldViolation[] | undefined;
}

export interface RouteDescriptor {
  method: string;
  path: string;
  handler: (req: Request) => Promise<Response>;
}

export interface OsnitServiceHandler {
  listFeed(ctx: ServerContext, req: ListFeedRequest): Promise<ListFeedResponse>;
  searchItems(ctx: ServerContext, req: SearchItemsRequest): Promise<SearchItemsResponse>;
  getItem(ctx: ServerContext, req: GetItemRequest): Promise<GetItemResponse>;
  listSources(ctx: ServerContext, req: ListSourcesRequest): Promise<ListSourcesResponse>;
}

export function createOsnitServiceRoutes(
  handler: OsnitServiceHandler,
  options?: ServerOptions,
): RouteDescriptor[] {
  return [
    {
      method: "GET",
      path: "/api/osnit/v1/list-feed",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: ListFeedRequest = {
            timeframe: params.get("timeframe") ?? "",
            regions: params.get("regions") ?? "",
            themes: params.get("themes") ?? "",
            sourceTypes: params.get("source_types") ?? "",
            from: Number(params.get("from") ?? "0"),
            to: Number(params.get("to") ?? "0"),
            verificationStatus: params.get("verification_status") ?? "",
            limit: Number(params.get("limit") ?? "0"),
            cursor: params.get("cursor") ?? "",
            tags: params.get("tags") ?? "",
            refresh: params.get("refresh") === "true",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listFeed", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listFeed(ctx, body);
          return new Response(JSON.stringify(result as ListFeedResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/osnit/v1/search-items",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: SearchItemsRequest = {
            q: params.get("q") ?? "",
            timeframe: params.get("timeframe") ?? "",
            regions: params.get("regions") ?? "",
            themes: params.get("themes") ?? "",
            sourceTypes: params.get("source_types") ?? "",
            from: Number(params.get("from") ?? "0"),
            to: Number(params.get("to") ?? "0"),
            verificationStatus: params.get("verification_status") ?? "",
            limit: Number(params.get("limit") ?? "0"),
            cursor: params.get("cursor") ?? "",
            tags: params.get("tags") ?? "",
            refresh: params.get("refresh") === "true",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("searchItems", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.searchItems(ctx, body);
          return new Response(JSON.stringify(result as SearchItemsResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/osnit/v1/get-item",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: GetItemRequest = {
            id: params.get("id") ?? "",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("getItem", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.getItem(ctx, body);
          return new Response(JSON.stringify(result as GetItemResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/osnit/v1/list-sources",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: ListSourcesRequest = {
            sourceType: params.get("source_type") ?? "",
            region: params.get("region") ?? "",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listSources", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listSources(ctx, body);
          return new Response(JSON.stringify(result as ListSourcesResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
  ];
}

