// Code generated by protoc-gen-ts-server. DO NOT EDIT.
// source: worldmonitor/legislative/v1/service.proto

export interface ListRecentNominalVotesRequest {
  dias: number;
  limit: number;
  offset: number;
}

export interface ListRecentNominalVotesResponse {
  votacoes: NominalVoteSession[];
  total: number;
  source: string;
  generatedAt: number;
}

export interface NominalVoteSession {
  votacaoId: string;
  dataHoraRegistro: number;
  dataHoraRegistroIso: string;
  siglaOrgao: string;
  descricao: string;
  resultado: string;
  tipoVotacao: string;
  aprovacao: number;
  proposicao?: LegislativeProposicao;
  votosCount: number;
  source: string;
}

export interface LegislativeProposicao {
  id: number;
  codigo: string;
  siglaTipo: string;
  numero: string;
  ano: number;
  ementa: string;
  uri: string;
}

export interface GetNominalVoteRollCallRequest {
  votacaoId: string;
}

export interface GetNominalVoteRollCallResponse {
  votacaoId: string;
  votos: NominalVoteRollCallEntry[];
  tally?: VoteTally;
  total: number;
  source: string;
  generatedAt: number;
}

export interface NominalVoteRollCallEntry {
  deputado?: LegislativeDeputado;
  voto: string;
}

export interface LegislativeDeputado {
  id: number;
  nome: string;
  siglaPartido: string;
  siglaUf: string;
  uri: string;
}

export interface VoteTally {
  favor: number;
  contra: number;
  abstencao: number;
  obstrucao: number;
  ausente: number;
  outros: number;
}

export interface ListDeputyRecentVotesRequest {
  deputadoId: number;
  limit: number;
  offset: number;
  scanPages: number;
  dias: number;
}

export interface ListDeputyRecentVotesResponse {
  items: DeputyVoteActivityItem[];
  total: number;
  source: string;
  generatedAt: number;
}

export interface DeputyVoteActivityItem {
  votacaoId: string;
  dataHoraRegistro: number;
  dataHoraRegistroIso: string;
  siglaOrgao: string;
  tipoVotacao: string;
  voto: string;
  titulo: string;
  source: string;
  proposicao?: LegislativeProposicao;
}

export interface SyncRecentNominalVotesRequest {
  dias: number;
  maxVotacoes: number;
  includeRollCalls: boolean;
}

export interface SyncRecentNominalVotesResponse {
  ok: boolean;
  votacoesSincronizadas: number;
  rollCallsSincronizados: number;
  erros: number;
  source: string;
  generatedAt: number;
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ServerContext {
  request: Request;
  pathParams: Record<string, string>;
  headers: Record<string, string>;
}

export interface ServerOptions {
  onError?: (error: unknown, req: Request) => Response | Promise<Response>;
  validateRequest?: (methodName: string, body: unknown) => FieldViolation[] | undefined;
}

export interface RouteDescriptor {
  method: string;
  path: string;
  handler: (req: Request) => Promise<Response>;
}

export interface LegislativeServiceHandler {
  listRecentNominalVotes(ctx: ServerContext, req: ListRecentNominalVotesRequest): Promise<ListRecentNominalVotesResponse>;
  getNominalVoteRollCall(ctx: ServerContext, req: GetNominalVoteRollCallRequest): Promise<GetNominalVoteRollCallResponse>;
  listDeputyRecentVotes(ctx: ServerContext, req: ListDeputyRecentVotesRequest): Promise<ListDeputyRecentVotesResponse>;
  syncRecentNominalVotes(ctx: ServerContext, req: SyncRecentNominalVotesRequest): Promise<SyncRecentNominalVotesResponse>;
}

export function createLegislativeServiceRoutes(
  handler: LegislativeServiceHandler,
  options?: ServerOptions,
): RouteDescriptor[] {
  return [
    {
      method: "GET",
      path: "/api/legislative/v1/list-recent-nominal-votes",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: ListRecentNominalVotesRequest = {
            dias: Number(params.get("dias") ?? "0"),
            limit: Number(params.get("limit") ?? "0"),
            offset: Number(params.get("offset") ?? "0"),
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listRecentNominalVotes", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listRecentNominalVotes(ctx, body);
          return new Response(JSON.stringify(result as ListRecentNominalVotesResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/legislative/v1/get-nominal-vote-roll-call",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: GetNominalVoteRollCallRequest = {
            votacaoId: params.get("votacao_id") ?? "",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("getNominalVoteRollCall", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.getNominalVoteRollCall(ctx, body);
          return new Response(JSON.stringify(result as GetNominalVoteRollCallResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "GET",
      path: "/api/legislative/v1/list-deputy-recent-votes",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: ListDeputyRecentVotesRequest = {
            deputadoId: Number(params.get("deputado_id") ?? "0"),
            limit: Number(params.get("limit") ?? "0"),
            offset: Number(params.get("offset") ?? "0"),
            scanPages: Number(params.get("scan_pages") ?? "0"),
            dias: Number(params.get("dias") ?? "0"),
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listDeputyRecentVotes", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listDeputyRecentVotes(ctx, body);
          return new Response(JSON.stringify(result as ListDeputyRecentVotesResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
    {
      method: "POST",
      path: "/api/legislative/v1/sync-recent-nominal-votes",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const body = await req.json() as SyncRecentNominalVotesRequest;
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("syncRecentNominalVotes", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.syncRecentNominalVotes(ctx, body);
          return new Response(JSON.stringify(result as SyncRecentNominalVotesResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
  ];
}

